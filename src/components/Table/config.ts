/**
 * Total width in pixels of items floating on the right side of the table
 */
export const leftPlaceholderWidth = 80;

/**
 * Width in pixels for all table cells
 */
export const cellWidth = 180;
